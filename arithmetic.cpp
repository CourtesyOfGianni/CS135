//Gianni Russell
//CSCI 135
//Prof Tomg Yi
//Project 1 task A

#include <iostream>
#include <cstdlib>
using namespace std;

int main(){
    int char operators[5] = {+, -, *, /, %};		//Declare and initialize an array of integers with the values +,-,*,/,%
    int size = sizeof(operators) / sizeof(operators[0]);

    int index = rand() % size;
    char selectedOp = operators[index];

    int num = rand() % 10; //generate a random int in [0,9]

    While(selectedOp == / || selectedOp == %) 
        char second op 

        
    }

}